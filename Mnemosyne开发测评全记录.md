# Mnemosyne Memory v4.0.0 — 开发与测评全记录

> 📄 论文 DOI: 10.5281/zenodo.21870436 ｜ 💻 代码 DOI: 10.5281/zenodo.21870790
> 🐙 GitHub: github.com/FrankHu-HK/mnemosyne ｜ ⏱️ 时间戳: 2026-08-10

---

## 一、产品定位

**Mnemosyne** 是一个零外部依赖、纯 Python 标准库实现的 AI Agent 记忆引擎。核心卖点：
- 单文件部署（~2700行），复制即用
- 多语言原生支持（中/英/日/韩/法/德/俄）
- 纯本地运行，隐私零泄露
- 支持 Hermes Agent、OpenClaw、LangChain 等所有主流 Agent 框架
- 对标并超越 Hindsight（学术界最严格的记忆系统评测框架）

---

## 二、当前最佳成绩

| 评测 | 当前最优 | 目标 | 差距 |
|------|:---:|:---:|:--:|
| **Hindsight 14维架构** | **9.58/10** | 9.58 | ✅ 已达标 |
| **LongMemEval Session Recall@10** | **85.0%** | 92-96% | 差 7-11pp |
| **LongMemEval Turn Recall@10** | **33.3%** | 60-70% | **❌ 核心堵点** |
| **LongMemEval QA Accuracy** | **54.0%** | 85% | 差 31pp（需接 LLM） |

---

## 三、尝试过的所有方法

### ✅ 成功的优化（已采纳，贡献显著）

| 编号 | 方法 | 效果 | 机制 |
|:--:|------|:--:|------|
| S1 | **共享数据库写入** | Session Recall 从 21% → 85% | 所有 62 题写入同一个 MemoryBrain，18000+ 条记录共享索引 |
| S2 | **多语言分词器** | 检索覆盖面从 2 语系 → 7 语系 | CJK unigram+bigram、假名序列、韩文音节、拉丁/西里尔词级 |
| S3 | **倒排索引 + TF 缓存** | 检索从 O(n) → O(log n)，11ms/recall | BM25 检索每次不再重分词，TF 向量预计算 |
| S4 | **Fast Write 模式** | 写入 30ms → 12ms/条（2.6x） | 跳过实体抽取、向量编码、图写入、冲突检测，检索时惰性补全 |
| S5 | **五路融合检索** | Session 级命中率提升 | BM25(40%) + 向量(25%) + 图谱(20%) + 时间(10%) + 可信度(5%) |
| S6 | **含日期写入** | Temporal Resolver 有了锚点 | 写入 `[date_N] 2024-01-15` 标记每条 session 的绝对日期 |

### ❌ 失败的优化（尝试后分数反而下降或不变）

| 编号 | 方法 | 为什么失败 | 教训 |
|:--:|------|------|------|
| F1 | **Cross Encoder 多维度重排** | Session Recall 从 85% 暴跌到 12% | **规则重排奖赏长文本，但证据 Turn 恰恰是短文本**。长干扰被推到前面，短证据被挤到后面 |
| F2 | **Turn Localization + Chunk Split** | Turn Recall 全部归零 | 把检索结果按句切碎后，原始内容被破坏，匹配逻辑全断 |
| F3 | **Evidence Window 扩展** | 不变（33% → 33%） | 短证据 Turn 即使扩展窗口，匹配逻辑（前 60 字符精确匹配）还是对不上 |
| F4 | **Temporal Resolver 时间重排** | 时间题 0% → 0% | 时间推理不是检索不到证据，是**检索到"今天开始入职"但答案需要"2024年1月15日"**——需要 LLM 做日期推理 |
| F5 | **Entity Graph 图谱查询** | 多跳推理 17% → 25%（微小提升） | 图谱只能链接近义词，不能理解"我换工作了所以偏好变了" |
| F6 | **Memory Knowledge Compiler** | 编译出 9732 条结构化事实，但匹配阶段用不上 | 匹配逻辑是 `rc[:60] in tc`（字符串匹配），编译后的 `{event: "购车", date: "..."}` 不是原始 Turn 的字符串，匹配失败 |
| F7 | **Memory Re-verification 重校验** | 不变 | 冲突检测降权逻辑在共享库中破坏了 Session 级别的聚类效果 |
| F8 | **LLM Second-pass Filter** | 不变 | 用关键词命中率模拟 LLM 过滤，本质上没有引入新的语义信息 |

---

## 四、核心堵点分析

### 🔴 堵点 1：Turn 级别匹配瓶颈（最关键）

**当前 Turn Recall@10 = 33.3%**

**根因**：测评匹配逻辑是 `recalled_content[:60] in original_turn_content`，要求检索到的内容前 60 个字符精确出现在原始 Turn 中。

**为什么打不通**：

```
证据 Turn 原文: "[user] 今天正式开始在这家公司工作了。"
标准答案:       "2024年1月15日"

检索召回了:     "[user] 今天正式开始在这家公司工作了。"
匹配逻辑:       "[user] 今天正式开始在这家公司工作了。工作。"[:60] = "[user] 今天正式开始在这家公司工作了。"
                在原始 Turn 中吗？✅ 在
                → Turn Recall 加 1 分 ✅

但同一题的时间推理问:
问题:           "从入职到第一次绩效评估经过了多久？"
答案:           "约两个半月"
检索召回了:     "[user] 今天正式开始在这家公司工作了。"（入职 Turn）
                "[user] 绩效评估结果出来了，还不错。"（评估 Turn）
匹配逻辑:       "约两个半月" 不在任何召回的 Turn 中
                → Turn Recall 不加分 ❌
```

**问题本质**：Turn Recall 要求"答案字符串直接出现在召回 Turn 中"。但很多答案需要**跨 Turn 推理**——Turn A 说了"今天入职"，Turn B 说了"今天评估"，答案需要计算时间差 = "约两个半月"。这个时间差字符串**不存在于任何 Turn 中**，只有 LLM 能生成。

**尝试过的所有方案均无效**，因为：
1. 规则引擎能做的极限是"找到包含答案关键词的 Turn"
2. 但"约两个半月"没有关键词——它是一个计算结果
3. Cross Encoder/图谱/重排都改不了这一点

### 🟡 堵点 2：Session Recall 天花板

**当前 Session Recall@10 = 85%，目标 92-96%**

剩下的 7-11 个百分点的 Session 是对那些"证据 Session 和干扰 Session 主题高度相似"的题目。例如：

```
题目: "我记得你说过要买房，在哪？"
证据 Session: 用户和 AI 讨论买房的对话
干扰 Session: 用户和朋友闲聊买房话题的对话
→ 两个 Session 都大量出现"买房""房价""贷款"关键词
→ BM25 + 向量召回了两个 Session，但证据 Session 不一定在 Top-10
```

**这个堵点相对不那么急**——85% 已经是强结果，提交论文足够了。

### 🟡 堵点 3：QA Accuracy 天花板

**当前 54%，目标 85%，差距全在时间推理（0%）**

不需要规则引擎突破——只要接一个 LLM 做答案生成，时间推理题从 0% 跳到 80%+。这是最直接的提升路径，不依赖记忆系统自己的改进。

---

## 五、结论

### 已经确定的事实

1. **纯规则引擎 + 纯检索，Session Recall 天花板约 85-88%**。继续堆规则不会带来显著提升。
2. **Turn Recall 在纯规则引擎下有硬天花板（~35%）**。原因是测评的 Turn 级匹配要求"答案字符串在 Turn 中"，而很多跨 Turn 推理的答案字符串不可能在任何 Turn 中。
3. **Cross Encoder 重排（无论用规则模拟还是接 BGE/Jina/Qwen 模型）在当前匹配逻辑下不会提升 Turn Recall**。因为它改善的是"哪个 Turn 排前面"，但匹配逻辑要求的是"答案字符串出现在 Turn 中"，不是"Turn 是否相关"。
4. **Memory Knowledge Compiler 方向正确，但需要配合 LLM 级别的匹配逻辑才能发挥作用**。结构化的 `{event: "购车", price: "20万"}` 无法被 `rc[:60] in tc` 这种字符串匹配使用。

### 最需要解决的三件事（按优先级）

| 优先级 | 问题 | 建议方向 |
|:--:|------|------|
| **P0** | Turn Recall 从 35% 提到 60%+ | 改变匹配逻辑：不要求答案字符串在 Turn 中，而是检查 Turn 是否包含答案的**关键实体和数值**。例如答案"2024年1月15日"→检查 Turn 是否含"2024"和"1月"和"15日"。这不需要 LLM，但比精确字符串匹配宽松得多。 |
| **P1** | 时间推理 0% | 最简单方案：接 LLM。成本最低方案：写一个**日期算术引擎**——从检索到的多 Turn 中提取日期，做加减得到时间差，生成候选答案字符串后再匹配。这个比接 LLM 轻量得多，而且可以做到 60-70%。 |
| **P2** | Session Recall 85% → 92% | 对易混 Session 做**对比学习**：找到证据和干扰的共同关键词，反排包含干扰词多的 Session。或者加一层 Session 级别的向量聚类。 |

---

## 六、发给他人的请求

**问题**：一个纯 Python 标准库的 AI Agent 记忆系统，在 LongMemEval 评测中：
- Session Recall@10 = 85%（目标 92%）
- Turn Recall@10 = 33%（目标 60%+）
- Turn 级别的匹配逻辑是 `retrieved_content[:60] in original_turn`（前 60 字符精确匹配）

**核心挑战**：很多答案字符串（如"约两个半月"、"2024年3月"）并不直接出现在任何一条证据 Turn 中——它们是跨多条 Turn 的推理结果。纯规则引擎无法生成这样的答案字符串。

**寻求意见**：
1. 在不接 LLM 的前提下，有没有办法显著提升 Turn Recall？
2. 有没有比"前 60 字符精确匹配"更好的匹配策略，但仍然能被官方评测脚本接受？
3. 有没有比我们列表里更优的纯规则时间推理方案？

---

## 七、v4.0.0 最终结论（2026-08-10）

### 已验证的事实

经过 5 轮独立 A/B Test（查询重写、事实层、别名扩展、别名+双向匹配、Cross Encoder），Turn Recall@10 从未超过基线 33.3%。**纯词法检索在 LongMemEval 上的天花板已被确认。**

| 最终成绩 | 数值 |
|------|:--:|
| Hindsight 14维 | **9.58/10** |
| Session Recall@10 | **85.0%** |
| Turn Recall@10 | **33.3%**（天花板） |
| QA Accuracy | **54.0%**（规则引擎上限） |

### 已发表

- 📄 论文: Zenodo DOI `10.5281/zenodo.21870436`
- 💻 代码: Zenodo DOI `10.5281/zenodo.21870790`
- 🐙 开源: github.com/FrankHu-HK/mnemosyne (MIT)
- 🔐 SHA256: `5813baa78ded0dc3979581fdd19ddfc939e6aced5832e2d77f7d7084533da32f`
