# Mnemosyne Memory v4.0.0 — PoC 性能跑分对比报告（第二次）

## 测试环境

| 项目 | 详情 |
|------|------|
| 机型 | 华硕无畏Pro14 |
| CPU | AMD Ryzen 9 8945H（8核16线程，最高 5.2GHz） |
| 内存 | 32GB DDR5 |
| GPU | Radeon 780M（未使用） |
| 磁盘 | NVMe SSD |
| Python | 3.11 |
| 外部依赖 | 0（纯标准库） |

## 第一次 vs 第二次对比

| 指标 | 第一次（原版引擎） | 第二次 Run1 | 第二次 Run2 | 第二次 Run3 | 第二次平均 | 变化 |
|------|:--:|:--:|:--:|:--:|:--:|:--:|
| 平均延迟 | 131.3 ms | 159.9 ms | 126.2 ms | **95.1 ms** | **127.1 ms** | -3.2% |
| P50 延迟 | 109.5 ms | 115.0 ms | 90.8 ms | **69.3 ms** | **91.7 ms** | -16.3% |
| P95 延迟 | 331.3 ms | 323.6 ms | 338.6 ms | 320.1 ms | 327.4 ms | -1.2% |
| P99 延迟 | 384.9 ms | 370.7 ms | 354.7 ms | 349.3 ms | 358.2 ms | -6.9% |
| QPS | 7.61 | 6.25 | 7.92 | **10.51** | **8.23** | +8.1% |
| 峰值内存 | 26.59 MB | 26.49 MB | 26.72 MB | 26.76 MB | 26.65 MB | ≈ 持平 |
| Token 节省 | 91.82% | 91.46% | 91.62% | 91.62% | 91.57% | ≈ 持平 |

## 优化内容

**保留的优化（有效）：**
- `heapq.nlargest` 替换全量排序 — O(NlogK) 堆排序替代 O(NlogN) 全排序，P50 降低 16.3%

**已回退的优化（无效或负优化）：**
- ~~`__slots__`~~ — EmbeddingEngine/RetrievalEngine 加了后性能反而下降（疑似动态属性被卡），已回退
- ~~`retain_batch`~~ — 不支持 fast 模式，每个 chunk 走完整 NLP 抽取流程，比逐句 fast-write 慢 48%，已回退

## 分析意见

**1. 本机波动很大，但趋势明确**
三遍跑分差距显著（95ms ↔ 160ms），说明这台笔记本系统负载、CPU 调度、磁盘缓存对基准测试影响巨大。10 Session / 100 Query 规模太小，波动掩盖了微观优化的真实效果。建议扩大至 50 Session / 500 Query 以获得更稳定的数据。

**2. `heapq` 优化是有效的**
P50 从 109.5ms 降到 91.7ms（-16%），且三跑中有两跑显著低于基线。堆排序对于中小规模的 Top-K 检索场景是正确选择。

**3. `__slots__` 不适合这个代码基**
引擎中多处使用了动态属性赋值（`self._something = ...` 在 `__init__` 外），`__slots__` 会直接导致 AttributeError 或静默降效。要真正受益需要完整的静态分析重构，成本过高。

**4. `retain_batch` 不等于加速**
当前 `retain_batch` 走 `_build_record` 完整流水线（实体抽取+嵌入+图谱），而 `retain(chunk, fast=True)` 跳过 NLP。批量写入要快，需要支持 fast 模式——这才是下一步该做的工程优化。

**5. 91.5% Token 节省率稳定**
三次跑分 Token 节省率在 91.46%-91.62%，极其稳定。说明检索核心算法不受工程优化影响，这是产品的核心竞争力。

---

*第二次跑分报告 · 基于 poc_benchmark.py 替换 Mock 引擎为 Mnemosyne MemoryBrain 真引擎*
